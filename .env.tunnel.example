# Use these overrides while the SSH tunnel is running:
# Adjust LOCAL_PORT to match scripts/pg_tunnel.sh (default 15432)

# Pooler via tunnel (recommended)
SUPABASE_DB_URL=postgresql://ai_analyst_copilot:YOUR_PASSWORD@127.0.0.1:15432/postgres?sslmode=require