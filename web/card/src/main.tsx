import React from 'react'
import ReactDOM from 'react-dom/client'
import AnalystCard from './AnalystCard.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AnalystCard />
  </React.StrictMode>,
)